server:
  port: 8081

spring:
  application:
    name: ticket-service

  config:
    import: optional:configserver:config-service

  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service

